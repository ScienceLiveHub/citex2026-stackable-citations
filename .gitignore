node_modules/
dist/
.DS_Store
*.log
.vite/
.cache/

# Office lock files (created when opening docx in Word)
~$*

# Local recording workspace — final mp4 goes to /demo/ and is committed
recordings/raw/
recordings/*.mov
recordings/*.tmp.*
